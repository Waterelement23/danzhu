# 发球线点选实施计划

用户已批准：移除发球滑条；轻点发球线附近选位置，场地拖动继续瞄准，默认中间且不增加确认步骤。当前 worktree 顺序实施。

- [x] 新增 `serve-selection.ts`，以投影后的地面线段计算点选位置，端点附近吸附至合法端点，远离线段返回空值。记录手势最大移动量，超过 5px 后即使回原点也不作为点选。先用 `tests/serve-selection.test.ts` 验证。
- [x] 新增 `serve-line.ts`，用轻量 SVG 标出实际可用发球线、短刻度及当前位置，点选与显示使用同一份投影。只在可操作的入场回合显示；沿真实地面高度投影，不额外引入渲染通道。
- [x] `scene.ts` 在按下时记录候选位置，抬手后才选择。拖动锁定原发球位置，取消/双指/离开回合清空候选；更新弹珠预览与高度投影，并回调 `main.ts` 同步最终发球参数。
- [x] `main.ts` 和 `style.css` 移除滑条及相关样式；保留等待房间的邀请/准备浮层。首次提示改为“点发球线选位置，拖动场地发射”，正常回合沿用拖动提示；发球提示独立于旧版引导记录。
- [x] 单元测试、生产构建与浏览器验证：横竖屏、左右端点、线外轻点、拖动不改位置、拖回取消不变位置、双指取消、后手入场、出手后的实际物理位置一致；正常回合不再可点选。更新既有手机回归脚本，执行真实联网回归。
- [x] 推送并部署，检查公网两端点选发球、资源/房间服务，记录部署及回滚镜像。

## 本地验证记录

- 106 项测试通过；一次与构建并行的全量运行中旧地形测试超过 5 秒，关闭额外浏览器负载后单独重跑全部通过，未修改测试限时或地形逻辑。生产构建通过。
- `scripts/mobile-direct-aim-check.js` 已更新为原生触摸点选：手机横竖屏的左右端点、22px 触摸容差、抬手生效、线外轻点、拖回取消、双指取消、两次入场、发球参数一致和正常回合隐藏选择线均通过。画布大小及 30/15fps、DPR 1.25 沿用上版。
- 桌面鼠标点选后使用精细瞄准发射，物理球初始 x 与选点一致（约 -0.61875）。
- 已更新并通过原浏览器冒烟脚本：双端点选发球、回合交接、相同随机地形、再战、断线结算。发球高度/轨迹脚本完成点选后预测变化、默认不显轨迹、取消后清除和后手无高位引导回归；旧脚本的加载等待补上空元素判断，测试前释放鼠标后通过。
- 发球线用同一份地面投影采样绘制和判定，只有可操作的发球方显示。普通回合无选点功能。

## 发布验证

代码 `00f6a9c` 已推送，部署镜像 `danzhu:20260911-serve-line`。公网七项冒烟检查及两个手机视口的实际选点/触摸发球通过：选点标记跟随点击位置、无滑条或发球面板、入场后选点线隐藏、双方独立镜头正确。页面无 JavaScript 错误；容器健康，Nginx 检查和热重载成功，现有 `/exam`、`/health` 继续返回 200。
