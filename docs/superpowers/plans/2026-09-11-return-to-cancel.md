# 手机退回起点取消实施计划

用户批准：删除顶部取消按钮，起点标记与手指附近的“松手取消”反馈；拉开可恢复，保留初次轻力度发射并避免状态抖动。

- [x] 提取 `return-cancel.ts` 状态机。首次拉动保留原 5px 死区和力度曲线；曾拉到 28px 后启用退回取消，回到起点 14px 内取消，拉出 22px 后恢复。用测试覆盖轻拉、回退、滞回边界、恢复、重置。
- [x] `scene.ts` 将状态机接入触摸手势，取消区内有效力度为零并立即清除轨迹；抬手重新检查最后坐标，清理时同步隐藏反馈。保持发球线轻点选择、桌面鼠标、镜头锁定及物理规则。
- [x] `main.ts`/`style.css` 删除顶部取消按钮与事件。只在有效触摸拖动时展示起点小标记，启用退回后提示“退回取消”；随手指移动的提示框显示力度或“松手取消”，限定在画面内且不截获触摸。取消区内结束蓄力音，拉出后恢复，取消不新增音效。
- [x] 原生触摸验证：初次弱发射、退回附近松手不发射、边界抖动保持取消、拉开恢复、没有末次 move 的抬手也取消、发球选点/取消不互相误判。手机横竖屏检查提示可读且位于手指附近，保留现有双指和系统取消检查。
- [ ] 完整单元测试与构建，线上双人验证后提交部署记录。真机手指遮挡程度与实际触感仍需用户体验确认。

本地验证：110 项测试通过，生产构建通过（原有大资源分包提示仍在）。原生触摸覆盖 390×844、844×390，均完成 3 次合法发射；取消区归零、滞回、重新拉开、抬手最终坐标、双指和系统取消通过。桌面与真实双人房间的发球、再战、断线回归通过，无页面错误。未进行真机手指遮挡和触感测量。
