# 出手朝向镜头实施计划

对应已批准的 `../specs/2026-09-11-turn-facing-camera-design.md`，在现有独立 worktree 内顺序实施。

- [x] 在 `src/client/turn-camera.ts` 提取回合镜头策略、水平朝向、58° 透视取景和最短转向计算；先在 `tests/turn-camera.test.ts` 覆盖发球例外、双方独立视角、运动保持、手动全景及 ±180° 边界。
- [x] 接入 `src/client/scene.ts`，替换固定方位和固定倍率。瞄准冻结实际姿态，运动仅拉远；重置、失去身份及窗口变化按设计处理。复用光学 BVH 选择无遮挡取景，否则回退全景并提示。
- [x] 修改 `src/client/match-ui.ts` 的触控方向转换，增加相机 yaw 参数，保留屏幕距离决定力度；测试四个主方向。`main.ts` 传入本地身份，提供桌面/手机共用语义的切换按钮和遮挡提示。
- [x] 检查八个水平朝向、两球贴边和手机横竖屏。若出现常见模型遮挡，调整安全取景并再次验证；不在转场中增加渲染通道。
- [ ] 执行完整 `npm test`、`npm run build` 和双浏览器房间操作回归；确认静态渲染预算保持。提交、部署并验证公网两端镜头状态与联机判胜。

物理世界、胜负与消息协议不改动。真实 iPhone 的温度和微信/Safari 手感由用户最终复测，桌面浏览器的移动视口不代替真机结论。

## 实现与验证记录

- 保持 58° 俯角，使用 0.5 秒平滑转场；拖动及松手后的网络等待立即锁定实际镜头。
- 复用现有遮挡查询，依次尝试安全取景；无法安全展示两球时回到全景。没有新增渲染通道或修改物理模型。
- 99 项测试通过，生产构建通过。透视取景使用 Three.js 相机投影独立校验。
- 两个独立手机视口的真实联网房间验证：两次入场、自己的出手视角、对方全景、手动切换、触控方向、转场中出手锁定和下一回合交接。横屏也完成房间回归；八个朝向与贴边取景已检查。复现脚本为 `scripts/turn-camera-check.js`（Playwright MCP 执行，连接本地开发页面）。
